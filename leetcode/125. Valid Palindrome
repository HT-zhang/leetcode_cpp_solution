#题目描述：
验证回文串
解法：
bool isPalindrome(string s) 
{
    int i = 0, j = s.length()-1;
    for(i ,j ; i<j; i++, j--)
    {
        while(!isalnum(s[i])&& i<j)
            i++;
        while(!isalnum(s[j] && i<j)
            j--;
        if(s[i] != s[j])
            return false;
     }
     return true;
}
